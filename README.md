## start

`npm run dev`

app start on port 4000

## server start

if you wanna test with server you need strt server on port 3000 or change config services/BaseService.ts

```
const defaultConfig = {
  baseURL: 'http://localhost:3000/',
};
```
